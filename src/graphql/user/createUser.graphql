mutation CreateUser($account: String!) {
  createUser(account: $account) {
   id
    claim_hash
    claim_signature
    claimed
    nonce
    amount
    claimed_at
    eligible
    account
  }
}