query GetUser($account: String!) {
  getUser(account: $account) {
    id
    claim_hash
    claim_signature
    nonce
    token
    claimed
    amount
    claimed_at
    eligible
    account
  }
}